<% content_for :title, "#{@conversation.customer.name} · Reservi" %>
<%= render partial: "conversation_detail", locals: { conversation: @conversation, messages: @messages, notes: @notes, panel_open: @panel_open } %>