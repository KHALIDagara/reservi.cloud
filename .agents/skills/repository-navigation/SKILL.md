---
name: repository-navigation
description: "Inspect and trace the Reservi repository before editing so agents reuse existing behavior, tests, and patterns instead of inventing parallel implementations."
---

# Antigravity adapter: repository-navigation

Before acting, read `.opencode/skills/repository-navigation/SKILL.md` completely and follow it as the canonical instructions for this skill.

Also follow the repository-root `AGENTS.md` and any closer `AGENTS.md`. If instructions conflict, use the precedence defined by the host and report any repository configuration drift.
