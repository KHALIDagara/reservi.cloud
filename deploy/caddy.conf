# Caddy configuration for reservi.cloud
# Place this in /etc/caddy/Caddyfile or as a snippet included from the main config.
#
# reservi.cloud {
#   reverse_proxy localhost:3000
# }
#
# After adding, reload: sudo systemctl reload caddy
echo "Place this in your Caddyfile:"
echo ""
echo "reservi.cloud {"
echo "  reverse_proxy localhost:80"
echo "}"