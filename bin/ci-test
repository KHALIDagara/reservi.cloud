#!/bin/bash
# Run the full test suite.
# Known pre-existing issues should be tracked separately, not suppressed here.
set -e
cd "$(dirname "$0")/.."

RAILS_ENV=test bin/rails db:test:prepare

RAILS_ENV=test PARALLEL_WORKERS=1 bundle exec rails test